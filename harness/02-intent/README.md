# Intent

## Como nomear specs

Convenção sugerida: `specs/<dominio>/<use-case>.md`, espelhada em `requirements/`, `acceptance-criteria/` e `plans/`.

## Como uma spec referencia o código

Cada spec deve linkar o caminho do código correspondente em `src/`.
