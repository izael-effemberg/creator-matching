# Copy to .env.local and fill in. Local values come from `npx supabase start`
# (printed at the end of its output, or `npx supabase status` afterwards).
# Never commit .env.local — it's gitignored.

SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_ANON_KEY=
